# Numerbook Calculator

> 一个现代化的文本式实时计算器

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Version](https://img.shields.io/badge/version-1.0.0-green.svg)

## ✨ 特点

- **实时计算** - 输入表达式，立即看到结果
- **变量支持** - 定义变量并在后续表达式中引用
- **示例模板** - 内置常用公式示例（BMI、房贷、折扣等）
- **简洁界面** - 精致的现代设计，专注计算体验
- **零依赖** - 纯原生 JavaScript，无需任何构建工具
- **键盘友好** - 完整的键盘快捷键支持

## 🚀 快速开始

### 本地使用

1. 克隆或下载本项目
2. 双击 `index.html` 打开
3. 开始计算！

### 在线访问

访问 [Vercel 部署链接] 即可在线使用。

## 📖 使用指南

### 快速开始

页面加载时会自动显示一个折扣计算示例，你可以直接修改数值进行计算：

```
原价 = 100        → 100
折扣 = 0.8        → 0.8
折后价 = 原价 * 折扣    → 80
```

### 基本计算

直接输入表达式，每行右侧会实时显示结果：

```
10 + 20         → 30
100 / 4         → 25
(1 + 2) * 3     → 9
-5 + 10         → 5
```

### 变量赋值

支持变量定义和引用：

```
a = 10          → 10
b = 20          → 20
c = a + b       → 30
d = c * 2       → 60
```

### 示例模板

点击右上角「示例」按钮，可以选择预设的公式模板：

| 模板 | 说明 | 示例变量 |
|------|------|---------|
| 📊 BMI 计算 | 身体质量指数 | 体重、身高、BMI |
| 🏠 房贷计算 | 等额本息月供 | 贷款、利率、年数、月供 |
| 🏷️ 折扣计算 | 商品打折 | 原价、折扣、折后价 |
| 💵 小费计算 | 餐厅小费 | 账单、小费比例、小费、总计 |
| 🌡️ 温度转换 | 摄氏度转华氏度 | 摄氏、华氏 |
| 💯 百分比计算 | 求百分比 | 总数、部分、百分比 |
| 🚗 速度计算 | 平均速度 | 距离、时间、速度 |
| 📈 复利计算 | 复利终值 | 本金、年利率、年数、本息合计 |

点击模板后，会自动填入公式和示例值，你可以修改数值查看不同结果。

### 中文变量名

支持中文变量名：

```
单价 = 99
数量 = 5
总计 = 单价 * 数量    → 495
```

## ⌨️ 快捷键

| 按键 | 功能 |
|------|------|
| `Enter` | 新增一行 |
| `Backspace` | 删除空行（光标在行首时） |
| `↑` | 聚焦上一行 |
| `↓` | 聚焦下一行 |

## 🧮 支持的运算符

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `+` | 加法 | `10 + 20` |
| `-` | 减法 | `20 - 10` |
| `*` | 乘法 | `6 * 7` |
| `/` | 除法 | `100 / 4` |
| `^` | 幂运算 | `2 ^ 10` |
| `()` | 括号优先级 | `(1 + 2) * 3` |
| `-` (一元) | 负号 | `-5` |

**注意**：计算结果最多保留 4 位小数。

## ❌ 错误处理

| 错误 | 说明 | 示例 |
|------|------|------|
| 变量 'x' 未定义 | 使用了未定义的变量 | `x + 1` |
| 除数不能为零 | 除以零 | `10 / 0` |

错误会以红色高亮显示，不影响其他行的计算。

## 📁 项目结构

```
numerbook-calculator/
├── index.html        # 主界面应用
├── calculator.js     # 计算器核心逻辑
├── test.html         # 浏览器测试套件
├── test-node.js      # Node.js 测试脚本
├── README.md         # 使用指南
└── PLAN.md           # 产品方案文档
```

## 🧪 测试

运行测试套件：

```bash
node test-node.js
```

或在浏览器中打开 `test.html` 查看测试结果。

## 📝 示例场景

### 购物计算

```
单价 = 99
数量 = 5
折扣 = 0.9
运费 = 10
总计 = 单价 * 数量 * 折扣 + 运费    → 455.5
```

### BMI 计算

```
身高 = 1.75
体重 = 70
BMI = 体重 / (身高 * 身高)    → 22.86
```

### 单位转换

```
美元 = 100
汇率 = 7.2
人民币 = 美元 * 汇率    → 720
```

## 🛠️ 技术实现

- **解析器**：手写递归下降解析器，正确的运算符优先级处理
- **变量管理**：基于 Map 的变量存储，支持依赖追踪
- **增量计算**：变量变化时只重新计算相关的行
- **UI 框架**：零框架，纯原生 HTML/CSS/JS

## 🔮 未来计划

- [ ] 数学函数支持（sin, cos, sqrt, log 等）
- [ ] 预设公式模板（BMI、房贷、折扣等）
- [ ] 保存/加载脚本（LocalStorage）
- [ ] 导出计算结果为文本/图片
- [ ] 深色/浅色主题切换

## 📄 许可证

MIT License

---

Made with ❤️ by Numerbook Team
